# Scripts

当前脚本按职责分组：

- `ci/`：GitHub Actions 和本地构建边界校验工具。
- `server/`：Server 本地预览、诊断和人工验证工具。

Desktop 构建、图标、签名和发布检查工具统一归属 `packaging/desktop/`，不在这里保留兼容转发脚本。历史版本发布清单已归档到 `docs/archive/release-checklists/`。
