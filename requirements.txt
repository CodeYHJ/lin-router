# Complete runtime dependencies for the Desktop-compatible local application.
# Server-only and packaging consumers should use the role-specific lists under
# requirements/ instead of installing this compatibility entry point.
-r requirements/desktop.txt
