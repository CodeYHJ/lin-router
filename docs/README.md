# Documentation

## 当前规范

- `docker-desktop-build-isolation.md`：Docker Server 与 Desktop 的目录、依赖、构建和验收边界。

## 历史归档

- `archive/backend-v0.6/`：v0.6 后端职责拆分、冻结与验收记录。
- `archive/prd/`：已经结束或被后续规范替代的 PRD。
- `archive/product/`：旧产品路线与背景材料。
- `archive/release-checklists/`：历史版本发布检查清单；其中的旧路径只描述当时版本，不是当前构建命令。

组件自己的使用说明保留在组件目录，例如 `frontend/README.md`。当前命令和构建入口以根 `README.md` 与当前规范为准。
